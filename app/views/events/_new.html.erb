<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <style>
      /****  TEMPORARY STYLE FOR CURRENT VERSION OF THE FORM ****/
      .field, .actions{
        display:block;
        float:left;
        padding-right:20px;
      }
      .actions{
        clear:both;
        margin-bottom: 15px;
      }
  </style>

  <div class="field">
    <%= f.label "Event Name" %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :time %><br />
    <%= f.text_field :time %>
  </div>

  <div class="field">
    <%= f.label :place %><br />
    <%= f.text_field :place %>
  </div>

  <div class="field">
    <%= f.label "Adminstrator Name" %><br />
    <%= f.text_field :admin %>
  </div>

  <div class="field">
    <%= f.label "Adminstrator Email" %><br />
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %> <br/><br/>
  </div>
<% end %>
